# C# - Test Driven Development

This project covers test-driven development (TDD) with C# using NUnit. Each task is a self-contained solution with a class library and a corresponding test library.

## Requirements

- All C# files compiled using `dotnet`
- Projects built with .NET 7 SDK