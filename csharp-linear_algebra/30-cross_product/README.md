# 30 - Cross Product

This task implements the cross product of two 3D vectors. The cross product is only defined in 3D; any input that is not a 3D vector yields a result of -1.

## Requirements

- All C# files compiled using `dotnet`
- Projects built with .NET 7 SDK
