# 24. Matrix shear 2D

Shears a 2D matrix along the X or Y axis. Each row is treated as a 2D point (x, y). The direction is case-insensitive.

## Requirements

- All C# files compiled using `dotnet`
- Projects built with .NET 7 SDK
