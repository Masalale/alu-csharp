# 18. Matrix multiplication

Multiplies two matrices using standard matrix multiplication. The number of columns in the first matrix must equal the number of rows in the second.

## Requirements

- All C# files compiled using `dotnet`
- Projects built with .NET 7 SDK
