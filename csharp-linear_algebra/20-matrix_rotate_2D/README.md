# 20. Matrix rotate 2D

Rotates a 2D matrix by applying a 2D rotation transformation to each row interpreted as a point (x, y). The angle is given in radians.

## Requirements

- All C# files compiled using `dotnet`
- Projects built with .NET 7 SDK
