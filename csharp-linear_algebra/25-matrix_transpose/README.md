# 25 - Matrix Transpose

This task implements a method that transposes a matrix by swapping its rows and columns. The matrix may be of any dimension or size; an empty input yields an empty result.

## Requirements

- All C# files compiled using `dotnet`
- Projects built with .NET 7 SDK
